export default function Footer() {
  return (
    <footer className="mt-auto border-t border-border-light py-6 text-center text-xs text-text-secondary">
      <p>
        Files are processed on-the-fly and never stored.&nbsp;
        <span className="font-medium text-text-primary">Lil-Badu</span> — compress &amp; convert
        images, fast.
      </p>
    </footer>
  );
}
